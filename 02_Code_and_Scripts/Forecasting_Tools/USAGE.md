# Forecasting Tools – Usage

Place Excel templates (XLSX) and any optional VBA macros in `macros/`.

## Export Path to Power BI
- Use a normalized table (long format) for Power BI ingestion.
- Consider a CSV export macro to a `./exports/` folder (excluded by .gitignore).
